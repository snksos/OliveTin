#
# Integration Test Config: General
#

showFooter: false
checkForUpdates: false

# Actions (buttons) to show up on the WebUI:
actions:
- title: Ping api-stage.minforcode.com
  shell: ping api-stage.minforcode.com -c 1
  icon: ping
